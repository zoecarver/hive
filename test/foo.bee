extern print(int)
extern filter(int*, int, def (int) -> int) -> int*

def isone int x -> int
    x - 1
end

def main int a
    tofilter = [1, 0, 1, 2, 6, 0]
    filtered = filter(tofilter, 6, isone) # 6 is the size of the array
    # print(filtered[1])
end